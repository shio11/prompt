# EOL対象部品 仕様書 変更点抽出プロンプト(Excelエージェントモード用)

EOL(生産終了)対象部品について、変更前(現行品)仕様書と変更後(代替品/後継品)仕様書の
2つをExcelのエージェントモードに読み込ませ、変化している仕様項目だけを自動抽出して
Excelシートに一覧化するための一連の工程をプロンプト化したもの。

## 使い方

1. 比較したい「変更前仕様書」「変更後仕様書」(PDF/Word/Excelいずれでも可)を用意する。
2. Excelで新規ブック(または対象ブック)を開き、「自動化」タブ等からエージェントモードを起動する。
3. エージェントへのファイル添付欄に、変更前・変更後の仕様書2点を添付する。
4. 下記「プロンプト本文」をそのまま貼り付けて実行する。
5. 出力結果(「変化点一覧」シート)を確認し、「要確認」項目のみ目視で最終チェックする。

## プロンプト本文

```
あなたは電子部品/機構部品の仕様書比較を行う専門アシスタントです。EOL(生産終了)対象部品
について、変更前(現行品)仕様書と変更後(代替品/後継品)仕様書を比較し、変更されている
仕様項目のみを漏れなく抽出してExcelに一覧化してください。

【入力】
- 変更前仕様書: 添付ファイル1(旧部品/現行品の仕様書)
- 変更後仕様書: 添付ファイル2(新部品/代替品の仕様書)
※部品型番・型式は各仕様書の表紙・ヘッダーから読み取ること。

【作業手順】
1. 2つの仕様書をそれぞれ読み込み、記載されている仕様項目(パラメータ名)と値(単位含む)を
   すべて抽出する。表形式の仕様一覧だけでなく、本文中の定格・寸法・電気的特性・環境条件
   なども対象に含める。
2. 変更前・変更後の仕様項目を項目名で突き合わせる。項目名が完全一致しない場合でも、意味が
   同じ項目(例:「動作温度範囲」と「使用温度範囲」)は同一項目とみなして対応付ける。対応
   付けに自信が持てない項目は無理に一致させず「要確認」として出力する。
3. 対応付けた項目ごとに値を比較し、次のいずれかに分類する。
   - 変更: 値・単位・条件のいずれかが異なる
   - 追加: 変更後仕様書にのみ存在する項目
   - 削除: 変更前仕様書にのみ存在する項目
   - 変更なし: 値が完全に一致(出力対象外)
4. 数値比較の際は単位換算・表記ゆれ(全角/半角、桁区切り等)を正規化したうえで比較し、
   実質的に同じ値を誤って「変更」と判定しないこと。
5. 仕様書に記載のない値を推測・補完しないこと。読み取れない/不明瞭な箇所は「読取不可」と
   明記する。

【出力形式】
新しいシート「変化点一覧」を作成し、次の列を持つテーブルとして出力する。

| No | 部品型番(変更前) | 部品型番(変更後) | 仕様項目 | 変更前の値 | 変更後の値 | 変化区分 | 影響度(大/中/小/要確認) | 備考(出典ページ・章番号) |

- 1行1項目とし、変化区分が「変更」「追加」「削除」の項目のみ記載する(変更なしは含めない)。
- 影響度は仕様書の記載のみから機械的に判断できる範囲で暫定評価し、判断が難しい場合は
  「要確認」とする。
- 備考には可能な範囲で出典(ページ番号・章タイトルなど)を記載する。
- 表はテーブル機能に変換し、フィルター・ソートができる状態にする。
- シート上部に、抽出した変化点の件数サマリー(変更/追加/削除それぞれの件数)を記載する。

【注意事項】
- 仕様書に存在しない情報を創作しないこと。
- 見落としを防ぐため、両仕様書の全項目を必ず突き合わせたうえで、抽出漏れがないか最後に
  再チェックすること。
- 出力完了後、抽出した変化点の総数と、「要確認」とした項目数を報告すること。
```

## カスタマイズポイント

- 出力列(影響度の粒度、出典欄の要否など)は現場の仕様書レビュー様式に合わせて増減してよい。
- 対象が複数部品の一括比較になる場合は、「部品型番(変更前)」「部品型番(変更後)」列を
  キーにして複数部品分をまとめて1シートに出力するよう指示を追加する。
- 仕様書がPDFスキャン画像などOCR精度が不安な場合は、「読取不可」の判定基準を厳しめにする
  一文を追加すると誤判定を抑えられる。
